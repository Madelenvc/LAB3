fetch('https://127.0.0.1:1337/api/Article')
  .then(response => {
    if (!response.ok) {
      throw new Error('No se pudo obtener los datos');
    }
    return response.json();
  })
  .then(data => {
    // Aquí puedes manipular los datos obtenidos
    console.log(data);
  })
  .catch(error => {
    console.error('Error al obtener los datos:', error);
  });
